---
permalink: /article.json
---
[
{%- for item in collections.articles -%}
		{
			"id": "{{ item.link }}",
			"title": "{{ item.title }}",
			"content": "{{ item.description }}",
			"link": "{{ item.link }}",
			"authorname": "{{ item.author.name }}",
			"authoruri": "{{ item.author.url }}",
			"updated": "{{ item.published }}"
		}
{% if not loop.last %},{% endif %}
{%- endfor %}
]
